import { MatedDirective } from './mated.directive';

describe('MatedDirective', () => {
  it('should create an instance', () => {
    const directive = new MatedDirective();
    expect(directive).toBeTruthy();
  });
});
